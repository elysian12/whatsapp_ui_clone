import 'package:flutter/material.dart';

Widget addStatus() {
  return Container(
    child: Stack(
      children: [
        CircleAvatar(backgroundColor: Colors.white,)
        
      ],
    ),
  );
}
